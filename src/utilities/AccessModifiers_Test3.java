package utilities;

import day40_FinalKeyword.ProtectedAccessModifier;

public class AccessModifiers_Test3 {

    public static void main(String[] args) {

      //  System.out.println(ProtectedAccessModifier.name1);//default access modifier can't been seen in outside package
      //  System.out.println(ProtectedAccessModifier.name2);//protected access modifier can't been seen in outside package, but if we inherent it we  CAN see it!!!

    }
}
